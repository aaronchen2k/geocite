# 默认开发配置。真实密钥只写入同目录的 .env.local（该文件不提交）。
HOST=127.0.0.1
PORT=8001
API_PREFIX=api/v1
DB_PATH=data/geocite.db
LOG_DIR=logs
LLM_API_KEY=

# 未来可选数据库配置：首期不安装也不加载 MySQL/PostgreSQL 驱动。
# MYSQL_HOST=127.0.0.1
# POSTGRES_HOST=127.0.0.1
