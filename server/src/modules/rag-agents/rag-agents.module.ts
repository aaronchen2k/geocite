import { Module } from '@nestjs/common'; import { TypeOrmModule } from '@nestjs/typeorm'; import { BrandEntity } from '../brands/brand.entity'; import { ModelEntity } from '../models/model.entity'; import { RagAgentEntity } from './rag-agent.entity'; import { RagAgentsController } from './rag-agents.controller'; import { RagAgentsService } from './rag-agents.service'; @Module({ imports: [TypeOrmModule.forFeature([RagAgentEntity, BrandEntity, ModelEntity])], controllers: [RagAgentsController], providers: [RagAgentsService] }) export class RagAgentsModule {}
