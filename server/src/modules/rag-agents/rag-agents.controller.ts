import { Body, Controller, Delete, Get, Param, ParseIntPipe, Patch, Post, Query } from '@nestjs/common'; import { CreateRagAgentDto, ListRagAgentDto, UpdateRagAgentDto } from './rag-agents.dto'; import { RagAgentsService } from './rag-agents.service';
@Controller('rag-agents') export class RagAgentsController { constructor(private readonly service: RagAgentsService) {} @Get() list(@Query() query: ListRagAgentDto) { return this.service.list(query); } @Post() create(@Body() dto: CreateRagAgentDto) { return this.service.create(dto); } @Get(':id') findOne(@Param('id', ParseIntPipe) id: number) { return this.service.findOne(id); } @Patch(':id') update(@Param('id', ParseIntPipe) id: number, @Body() dto: UpdateRagAgentDto) { return this.service.update(id, dto); } @Delete(':id') remove(@Param('id', ParseIntPipe) id: number) { return this.service.remove(id); } }
