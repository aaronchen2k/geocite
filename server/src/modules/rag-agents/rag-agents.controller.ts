import { Body, Controller, Delete, Get, Param, ParseIntPipe, Patch, Post } from '@nestjs/common'; import { CreateRagAgentDto, UpdateRagAgentDto } from './rag-agents.dto'; import { RagAgentsService } from './rag-agents.service';
@Controller('rag-agents') export class RagAgentsController { constructor(private readonly service: RagAgentsService) {} @Get() list() { return this.service.list(); } @Post() create(@Body() dto: CreateRagAgentDto) { return this.service.create(dto); } @Get(':id') findOne(@Param('id', ParseIntPipe) id: number) { return this.service.findOne(id); } @Patch(':id') update(@Param('id', ParseIntPipe) id: number, @Body() dto: UpdateRagAgentDto) { return this.service.update(id, dto); } @Delete(':id') remove(@Param('id', ParseIntPipe) id: number) { return this.service.remove(id); } }
