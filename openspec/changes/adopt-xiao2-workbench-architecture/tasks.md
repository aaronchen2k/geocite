## 1. 项目与运行时基础

- [ ] 1.1 建立后端/前端工作区结构，配置 NestJS、Next.js、SQLite/TypeORM、类型共享和环境配置约定。
- [ ] 1.2 新增 Project 实体、迁移、仓储、服务和 API，支持项目状态、根目录、默认项目、软删除和执行提供者配置。
- [ ] 1.3 新增 ModelConfig 实体、管理 API 和前端配置页，支持启用、唯一默认模型与默认模型删除保护。
- [ ] 1.4 实现项目根目录与迭代目录名称校验，覆盖路径穿越、不可访问根目录和归档项目禁止执行场景。
- [ ] 1.5 实现项目工作区上下文、项目列表/选择界面和项目状态展示。

## 2. 迭代目录和产物服务

- [ ] 2.1 实现 `<projectRoot>/specs/<iterationDir>/` 的创建、列举和迭代摘要服务。
- [ ] 2.2 定义迭代产物契约，支持设计资产、阶段工件、输出文件和任务进度读取。
- [ ] 2.3 实现迭代相对路径读取与下载 API，增加根目录边界和文件类型/不存在错误处理。
- [ ] 2.4 提供原子文件写入工具，覆盖 JSON、YAML、Markdown 写入失败时保留旧内容的测试。
- [ ] 2.5 实现迭代详情页的阶段导航、产物浏览和下载入口。

## 3. Speckit 工作流状态机

- [ ] 3.1 定义 kickoff、constitution、specify、plan、tasks、implement 的阶段类型、前置关系、命令映射和持久化快照结构。
- [ ] 3.2 实现工作流启动、阶段前置校验、状态推进、运行 ID 和结构化成功/失败结果落盘。
- [ ] 3.3 实现阶段产物校验，禁止缺少必需工件或非结构化命令结果时推进状态。
- [ ] 3.4 实现刷新/重启后的运行快照恢复，并在迭代详情页恢复当前阶段和错误信息。
- [ ] 3.5 为顺序执行、缺失前置、失败持久化、过期运行与原子状态写入添加后端测试。

## 4. 执行提供者与命令协议

- [ ] 4.1 定义 ExecutionProvider 接口、统一命令输入/输出、取消信号和标准错误类型。
- [ ] 4.2 实现 local 提供者，受控地在项目根目录执行 Speckit/Codex 命令并收集退出码与输出摘要。
- [ ] 4.3 实现 Docker 和 OpenSandbox 提供者适配层及不可用状态；首期未配置的提供者必须返回明确错误。
- [ ] 4.4 将模型解析、敏感配置注入和命令协议校验接入工作流服务，禁止在日志/快照中输出凭据。
- [ ] 4.5 为默认模型、提供者不可用、命令取消、协议异常和退出失败添加集成测试。

## 5. 日志、状态推送与恢复

- [ ] 5.1 实现项目级、迭代级、阶段级结构化日志与敏感头字段脱敏。
- [ ] 5.2 实现阶段日志 SSE：初始 snapshot、增量 line、keep-alive、断连清理和历史日志读取。
- [ ] 5.3 实现工作流 WebSocket 状态推送、运行订阅、检查问题响应和前端实时状态更新。
- [ ] 5.4 以项目/迭代/阶段组合建立并发锁与 runId 校验，支持取消并防止旧运行覆盖当前状态。
- [ ] 5.5 实现回退到 specify/plan、从合法阶段重做及检查答案绑定重跑，按依赖关系清理过期状态。
- [ ] 5.6 为 SSE、WebSocket、脱敏、并发冲突、回退/重做和检查恢复增加 smoke/端到端测试。

## 6. 项目级收尾与交付文档

- [ ] 6.1 实现可纳入迭代判定、排除原因和项目级收尾状态/日志持久化。
- [ ] 6.2 实现技术能力声明合并与代码/依赖/锁文件/部署/ORM/数据库配置证据扫描。
- [ ] 6.3 生成并校验 `.specify/project-tech-stack.json`，对声明与实际实现冲突阻断发布。
- [ ] 6.4 实现 quickstart 和环境安装文档的临时生成、内容校验和整体原子发布。
- [ ] 6.5 新增项目收尾页面，展示纳入/排除迭代、技术栈摘要、日志、运行状态、错误与恢复建议。
- [ ] 6.6 为成功发布、冲突阻断、生成失败保留旧文档和页面恢复状态添加自动化验证。

## 7. GEO 业务接入与质量门禁

- [ ] 7.1 将现有 GEO 监测、归因和报告变更接入项目 ID、项目根、执行运行记录、日志和产物访问边界。
- [ ] 7.2 明确 GEO 外部平台适配器与 Speckit 执行提供者的独立配置、错误处理和审计日志边界。
- [ ] 7.3 为项目工作台与 GEO 领域模块之间的 API/类型契约添加集成测试，验证互不解释对方领域状态。
- [ ] 7.4 运行后端单元/集成/smoke 测试、前端构建和关键 Playwright E2E，覆盖项目创建、迭代执行、实时日志、恢复与项目收尾。
- [ ] 7.5 补充部署与运维文档：运行时提供者启用条件、密钥管理、目录挂载、日志保留与故障恢复流程。
