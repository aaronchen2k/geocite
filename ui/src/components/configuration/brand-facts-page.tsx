'use client'; import {useTranslations} from 'next-intl'; import {Button} from '@/components/ui/button'; import {useWorkspaceStore} from '@/stores/workspace-store';
export function BrandFactsPage():React.JSX.Element {const t=useTranslations('BrandFacts');const brandId=useWorkspaceStore(s=>s.currentBrandId);return <section className="max-w-3xl pb-8"><header className="mb-6 border-b border-[var(--border)] pb-4"><h1 className="mb-2 text-[22px] font-semibold">{t('title')}</h1><p className="text-sm text-[var(--muted-foreground)]">{t('description')}</p></header>{!brandId?<p className="text-sm text-[var(--muted-foreground)]">{t('noBrand')}</p>:<div className="rounded-lg border border-[var(--border)] bg-[var(--card)] px-6 py-14 text-center"><h2 className="font-semibold">{t('emptyTitle')}</h2><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-[var(--muted-foreground)]">{t('emptyDescription')}</p><Button className="mt-5" disabled>{t('add')}</Button></div>}</section>;}
