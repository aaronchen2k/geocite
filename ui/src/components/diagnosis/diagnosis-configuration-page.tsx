'use client';

import {Icon} from '@iconify/react';
import {useEffect, useMemo, useState} from 'react';
import {useTranslations} from 'next-intl';
import {Button} from '@/components/ui/button';
import {requestJson} from '@/lib/api';
import {useWorkspaceStore} from '@/stores/workspace-store';

type Market = 'cn' | 'global' | 'both';
type Question = {id: number; text: string; group: string; market: Market; brandProbe: boolean};
type Configuration = {questions: Question[]; prompt: string};
const groups = ['推荐', '比较', '替代', '价格', '风险', '品牌验证', '场景'];

export function DiagnosisConfigurationPage(): React.JSX.Element {
  const t = useTranslations('QuestionBank');
  const brandId = useWorkspaceStore((state) => state.currentBrandId);
  const [questions, setQuestions] = useState<Question[]>([]);
  const [prompt, setPrompt] = useState('');
  const [filter, setFilter] = useState<string | null>(null);
  const [saving, setSaving] = useState(false);
  const [error, setError] = useState('');
  const [saved, setSaved] = useState(false);
  const [generated, setGenerated] = useState<string[]>([]);
  useEffect(() => { setQuestions([]); setGenerated([]); setError(''); if (!brandId) return; void requestJson<Configuration>(`brands/${brandId}/diagnosis-questions`).then((data) => { setQuestions(data.questions); setPrompt(data.prompt); }).catch((reason: unknown) => setError(reason instanceof Error ? reason.message : t('loadFailed'))); }, [brandId, t]);
  const shown = useMemo(() => filter ? questions.filter((question) => question.group === filter) : questions, [filter, questions]);
  const update = (id: number, patch: Partial<Question>) => setQuestions((items) => items.map((item) => item.id === id ? {...item, ...patch} : item));
  const add = () => setQuestions((items) => [...items, {id: -Date.now(), text: '', group: '推荐', market: 'cn', brandProbe: false}]);
  const remove = (id: number) => setQuestions((items) => items.filter((item) => item.id !== id));
  const save = async () => { if (!brandId) return; setSaving(true); setError(''); try { const configuration = await requestJson<Configuration>(`brands/${brandId}/diagnosis-questions`, {method: 'PUT', headers: {'content-type': 'application/json'}, body: JSON.stringify({questions, prompt})}); setQuestions(configuration.questions); setPrompt(configuration.prompt); setSaved(true); } catch (reason) { setError(reason instanceof Error ? reason.message : t('saveFailed')); } finally { setSaving(false); } };
  const generate = async () => { if (!brandId) return; setError(''); try { const response = await requestJson<{questions: string[]}>(`brands/${brandId}/diagnosis-questions/generate`, {method: 'POST', headers: {'content-type': 'application/json'}, body: JSON.stringify({prompt})}); setGenerated(response.questions); } catch (reason) { setError(reason instanceof Error ? reason.message : t('generateFailed')); } };
  const addGenerated = (text: string) => { if (!questions.some((item) => item.text === text)) setQuestions((items) => [...items, {id: -Date.now(), text, group: '推荐', market: 'cn', brandProbe: false}]); };
  return <section className="pb-8"><header className="mb-6 border-b border-[var(--border)] pb-4"><h1 className="mb-2 text-[22px] font-semibold">{t('title')}</h1><p className="max-w-3xl text-sm leading-6 text-[var(--muted-foreground)]">{t('description')}</p></header>{!brandId ? <p className="text-sm text-[var(--muted-foreground)]">{t('noBrand')}</p> : <>{error && <p role="alert" className="mb-4 rounded-md border border-red-200 bg-red-50 px-3 py-2 text-sm text-red-700">{error}</p>}{saved && <p className="mb-4 rounded-md border border-emerald-200 bg-emerald-50 px-3 py-2 text-sm text-emerald-700">{t('saved')}</p>}<div className="mb-4 flex flex-wrap items-center justify-between gap-3"><p className="text-sm text-[var(--muted-foreground)]">{t('count', {count: shown.length})}</p><div className="flex gap-2"><Button size="sm" variant="outline" onClick={add}><Icon icon="lucide:plus"/>{t('add')}</Button><Button size="sm" disabled={saving} onClick={() => void save()}>{saving ? t('saving') : t('save')}</Button></div></div><div className="mb-4 grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{groups.map((group) => { const total = questions.filter((item) => item.group === group).length; return <button key={group} type="button" onClick={() => setFilter(filter === group ? null : group)} className={`rounded-lg border p-3 text-left text-sm ${filter === group ? 'border-[var(--primary)] bg-[var(--muted)]' : 'border-[var(--border)] bg-[var(--card)] hover:bg-[var(--muted)]'}`}><span className="font-medium">{group}</span><span className="ml-2 text-[var(--muted-foreground)]">{t('groupCount', {count: total})}</span><p className="mt-1 text-xs text-[var(--muted-foreground)]">{group === '品牌验证' ? t('probeHint') : t('groupHint')}</p></button>; })}</div><div className="overflow-x-auto rounded-lg border border-[var(--border)]"><table className="w-full min-w-[700px] table-fixed text-left text-sm"><colgroup><col/><col className="w-[150px]"/><col className="w-[150px]"/><col className="w-[80px]"/><col className="w-[52px]"/></colgroup><thead className="bg-[var(--muted)]"><tr><th className="px-3 py-3">{t('question')}</th><th className="px-3 py-3">{t('group')}</th><th className="px-3 py-3">{t('market')}</th><th className="px-3 py-3">{t('probe')}</th><th className="px-3 py-3"/></tr></thead><tbody>{shown.map((question) => <tr key={question.id} className="border-t border-[var(--border)]"><td className="px-3 py-2"><input aria-label={t('question')} className="h-9 w-full rounded-md border border-[var(--border)] bg-[var(--card)] px-3" value={question.text} onChange={(event) => update(question.id, {text: event.target.value})}/></td><td className="px-3 py-2"><select aria-label={t('group')} className="h-9 rounded-md border border-[var(--border)] bg-[var(--card)] px-2" value={question.group} onChange={(event) => update(question.id, {group: event.target.value})}>{groups.map((group) => <option key={group}>{group}</option>)}</select></td><td className="px-3 py-2"><select aria-label={t('market')} className="h-9 rounded-md border border-[var(--border)] bg-[var(--card)] px-2" value={question.market} onChange={(event) => update(question.id, {market: event.target.value as Market})}><option value="cn">{t('cn')}</option><option value="global">{t('global')}</option><option value="both">{t('both')}</option></select></td><td className="px-3 py-2"><input aria-label={t('probe')} type="checkbox" checked={question.brandProbe} onChange={(event) => update(question.id, {brandProbe: event.target.checked})}/></td><td className="px-3 py-2"><Button size="icon" variant="ghost" aria-label={t('remove')} onClick={() => remove(question.id)}><Icon icon="lucide:trash-2"/></Button></td></tr>)}{!shown.length && <tr><td colSpan={5} className="px-3 py-10 text-center text-[var(--muted-foreground)]">{t('empty')}</td></tr>}</tbody></table></div><section className="mt-5 rounded-lg border border-[var(--border)] bg-[var(--card)] p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="font-semibold">{t('generateTitle')}</h2><p className="mt-1 text-sm text-[var(--muted-foreground)]">{t('generateDescription')}</p></div><Button size="sm" variant="outline" onClick={() => void generate()} disabled={!prompt.trim()}><Icon icon="lucide:sparkles"/>{t('generate')}</Button></div><textarea aria-label={t('prompt')} className="mt-4 min-h-48 w-full rounded-md border border-[var(--border)] bg-[var(--card)] p-3 text-sm" value={prompt} onChange={(event) => setPrompt(event.target.value)} placeholder={t('promptPlaceholder')}/>{generated.length > 0 && <div className="mt-4 space-y-2 border-t border-[var(--border)] pt-4">{generated.map((text) => <div key={text} className="flex items-center justify-between gap-3 rounded-md bg-[var(--muted)] px-3 py-2 text-sm"><span>{text}</span><Button size="sm" variant="outline" onClick={() => addGenerated(text)}>{t('add')}</Button></div>)}</div>}</section></>}</section>;
}
